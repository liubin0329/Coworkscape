<div class="page-header">
<h2>新規登録</h2>
</div>

<p>Facebookのアカウントをお持ちの方なら1クリックで登録できます。承認なくFacebookに投稿することはありません。<br />
<small>※初回のみ、Facebookにご登録のメールアドレスに確認のメールをお送りしますので、メールに記載のリンクをクリックしてアカウントを有効にしてください。</small><br /></p>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
  <div class="well"> 
  <a href="/terms" target="_blank">利用規約</a>に同意の上、<%= link_to image_tag('/assets/signin_facebook.png', alt: "Sign in with #{provider.to_s.titleize}"), omniauth_authorize_path(resource_name, provider) %>
  </div>
  <% end -%>
<% end -%>

<p>もしくは以下をご入力の上、登録してください。</p>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => "form-horizontal" }) do |f| %>
  <%= devise_error_messages! %>
  <div class="control-group">
  <%= f.label :email, :class => "control-label" %>
  <div class="controls">
  <%= f.email_field :email, :class=>"input-file" %>
  </div>
  </div>
  
  <div class="control-group"><%= f.label :name, :class => "control-label" %>
  <div class="controls">
  <%= f.text_field :name, :class=> "input-file" %>
  </div>
  </div>
  
  <div class="control-group">
  <%= f.label :password, :class => "control-label" %>
  <div class="controls">
  <%= f.password_field :password, :class=>"input-file" %>
  </div>
  </div>

  <div class="control-group">
  <%= f.label :password_confirmation, :class => "control-label" %>
  <div class="controls">
  <%= f.password_field :password_confirmation, :class=>"input-file" %>
  </div>
  </div>

    <div class="form-actions">
        <a href="/terms" target="_blank">利用規約</a>に同意の上、<%= f.submit '新規登録', :class=>"btn btn-primary" %>
    </div>
<% end %>

<%= render "links" %>